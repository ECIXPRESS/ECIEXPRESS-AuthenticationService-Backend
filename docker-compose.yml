version: "3.9"

services:
  authentication-service:
    build: .
    container_name: authentication-service
    ports:
      - "8082:8082"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    networks:
      - eciexpress-network

networks:
  eciexpress-network:
    driver: bridge